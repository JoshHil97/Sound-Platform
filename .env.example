# Local development (SQLite)
DATABASE_URL="file:./dev.db"

# Production (Vercel + Postgres) — set this in the Vercel project's
# Environment Variables UI, not in a committed .env file. See
# docs/phase-13/LAUNCH_RUNBOOK.md for the full setup sequence.
# DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"
